<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo01.dao.CalculateDao">


    <insert id="inputPurchase">
        INSERT INTO  recharge (RECHARGE,RECHARGE_DATE) VALUES (#{recharge},#{rechargedate})
    </insert>

    <insert id="inputPlanting">
        INSERT INTO plantings(
        ID,
        PLANT_NAME,
        ESTIMATE_ALL_CALLBACK_INCOME,
        PLANT_NUMBER,PLANT_DATE,
        ESTIMATE_PULLOUT_DATE,IS_PULLOUT
        )
        VALUES(
        #{plant.id},
        #{plant.plant_name},
        #{plant.estimate_all_callback_income},
        #{plant.plant_number},
        #{plant.plant_date},
        #{plant.estimate_pullout_date},
        #{plant.is_pullout})
     </insert>


    <select id="getSeedsInfo" resultType="com.example.demo01.entity.SeedsEntity">
        SELECT * FROM seeds s WHERE s.PLANT_NAME LIKE '%${plantname}'
    </select>


</mapper>